server:
  port: 8084

spring:
  application:
    name: vision-project
  datasource:
    url: jdbc:postgresql://localhost:5432/vision_paas
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
