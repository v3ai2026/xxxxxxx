spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/visiondb
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

logging:
  level:
    com.vision: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
